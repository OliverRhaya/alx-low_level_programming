#include<stdio.h>
/**
 * main - Commencement point
 * Return: Always 0 (Accepted)
 */

int main(void)
{
	char c;

	c = 'z';
	while
		(c >= 'a') {
			putchar(c);
			c--;
		}
	putchar('\n');
	return (0);
}
