#include<stdio.h>
/**
 * main - Commencement point
 * Return: Always 0 (Accepted)
 */

int main(void)
{
	int x;

	for (x = 0; x < 10; a++)
	{
		putchar(x + '0');
		if (x < 9)
		{
			putchar(',');
			putchar(' ');
		}
		putchar('\n');
		return (0);
	}
